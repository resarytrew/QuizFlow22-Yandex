name: CI
on:
  push:
    branches: [main, feat/razgovory-o-vazhnom-template]
  pull_request:
    branches: [main, feat/razgovory-o-vazhnom-template]
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  checks:
    uses: ./.github/workflows/verify.yml
